run net_server.py first
after running net_server.py, run net_client.py
there is a loop in net_client.py, it will send messages 10 times.
The client programm will print a string like "Send: [x] time" each time sending the message, and will print "Recive: [x]" each time reciving the message. The server program will print the message everytime it recive the sending massage from client.
